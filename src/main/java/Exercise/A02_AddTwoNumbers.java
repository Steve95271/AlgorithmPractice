package Exercise;

public class A02_AddTwoNumbers {

    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {
/*        ListNode head = new ListNode(-1, null);
        ListNode tail = new ListNode();



        while (l1.next != null && l2.next != null) {
           int i = l1.val + l2.val;

            if (i > 9) {
                if (head.val == -1) {
                    head.val = 1;
                    head.next = new ListNode(i % 10);
                    tail = resultListNode.next;
                }
            }else {
                resultListNode.val = i;
                resultListNode.next = new ListNode();
            }



            //First linked list null
            if (l1.next != null) {
                l1 = l1.next;
            }else {
                resultListNode.val = 0 + l2.val;
            }

            //Second linked list null
            if (l2.next != null) {
                l2 = l2.next;
            }else {
                resultListNode.val = l1.val + 0;
            }


        }

        return resultListNode;*/
        return null;
    }
}
